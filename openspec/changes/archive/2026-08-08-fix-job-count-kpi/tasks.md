## 1. Overview KPIs

- [x] 1.1 Calculate the number of non-overlapping historical applications in `apps_df` (if any).
- [x] 1.2 Update the "Total Jobs Tracked" metric calculation in `overview.py` to add the length of the live `jobs` dataframe and the non-overlapping historical apps.
- [x] 1.3 Update the metric's tooltip to explain that it includes both live tracked jobs and historical/manual applications.
